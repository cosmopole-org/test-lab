# Build outputs and caches across all subprojects
caspar/node/appengine/target/
caspar/node/appengine/appletdb/
caspar/node/build/
decillionai-cli/node_modules/
decillionai-cli/dist/
decillionai-server/wasm/

# Local state from manual CLI testing (workdir's `auth/` and `files/`
# live under the user's working directory at runtime, not in the repo)
/auth/
/files/

# Editor / OS noise
.DS_Store
*.swp
